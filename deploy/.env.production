# =============================================================================
# Salon Flow API - Production Environment Configuration
# =============================================================================
# This file is a template. Actual values are stored in GCP Secret Manager.
# =============================================================================

# Application
ENVIRONMENT=production
APP_NAME=Salon Flow API
DEBUG=false

# GCP Project
FIREBASE_PROJECT_ID=salon-saas-487508

# Redis (Upstash - Serverless Redis)
# Format: redis://default:PASSWORD@HOST:PORT
# Or for Upstash: rediss://default:PASSWORD@HOST:PORT (TLS enabled)
UPSTASH_REDIS_URL=rediss://default:xxx@xxx.upstash.io:6379

# JWT Configuration
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING
JWT_ALGORITHM=HS256
JWT_EXPIRATION=3600
JWT_REFRESH_EXPIRATION=604800

# OpenRouter AI
OPENROUTER_API_KEY=sk-or-xxx

# Twilio (Optional - for SMS notifications)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# CORS (Comma-separated origins)
CORS_ORIGINS=["https://salon-flow.web.app","https://salon-flow.firebaseapp.com"]

# Business Settings
GST_RATE=5.0
LOYALTY_POINTS_PER_RUPEE=1
LOYALTY_POINT_VALUE=1.0
LOYALTY_POINTS_EXPIRY_MONTHS=12
MEMBERSHIP_RENEWAL_DAYS=15

# Booking Settings
DEFAULT_SLOT_DURATION=30
MAX_FUTURE_BOOKING_DAYS=30
CANCELLATION_HOURS=2
LATE_ARRIVAL_MINUTES=15
